# Community Spark — Roadmap

Living list of what's next. Update this file whenever priorities shift or something gets done — this is the source of truth across chat sessions, not just conversation history.

- [x] Restyled the per-event add-ons as pill/chip toggles matching events.html's filter buttons (2026-08-24) — user asked for the event-filter look specifically. Reused the exact `.filter-btn` visual language (white/muted → solid ink fill when active) via a new `.addon-chip`/`.addon-chip-group` CSS pattern (checkbox + label, input visually hidden and overlaid, `input:checked~span` swaps the fill — no `:has()` needed, broad browser support). Scoped to just the per-event add-ons, which are short single-line items and a clean fit; the 4 general packages (which show a price + description line) kept the priced-row layout from the previous formatting pass, since that content doesn't fit a compact chip — confirmed this split with the user before building. Verified at 375/768/1100px: correct chip toggle behavior, zero overflow, zero new console errors.

- [x] Reformatted the booking add-ons checklists (2026-08-24) — the General Add-On Packages list originally buried its price inline in the label text ("Décor Refresh — $295"), same row style as the unpriced per-event add-ons. Gave packages their own layout: bold name + gray detail line on the left, price on the right in the site's serif gold-numeral style (matching `pricing.html`'s own bundle cards), single column with hairline dividers between items instead of the 2-column grid used for the plain per-event add-ons (which stayed as-is — they're short, unpriced, and didn't need the change). Verified at 375/768/1100px: correct layout, zero overflow, zero new console errors, all prior functional behavior (show/hide, event switching, checkbox state) still intact.

- [x] Clarified pricing/removal expectations on the new booking add-ons checklists (2026-08-24) — updated the helper text under both checklists on `book.html` to explicitly state: selecting an item doesn't commit you to it, real pricing arrives in the written proposal, and anything that doesn't fit the budget can be dropped before approving. Matches the existing site-wide pattern (nothing is a binding commitment until the proposal is approved) but states it plainly right where someone is clicking checkboxes, instead of relying on them to already know that from elsewhere on the page.

- [x] Added selectable add-ons to the booking form (2026-08-24) — on `book.html`, right after "What Would You Like to Add or Change About This Event?", added two checkbox checklists: (1) that event's own "Optional add-ons" list (the same 6 items shown on its `events.html` card, now dynamically rendered per selection via a new `event-addons-data.js` data file — extracted programmatically from `events.html` to avoid transcription drift), and (2) the 4 general packages from `pricing.html`'s Add-on packages section (with price + a one-line detail). Per-event list only shows for Preset Event bookings and updates live when the event dropdown changes; the general packages list shows for both Preset and Custom bookings, since those apply to any event. Selections stay unpriced (matches how add-ons work everywhere else on the site) — they're just checkboxes that get included in the form submission for Griffin to quote manually. Verified: correct show/hide on event selection and the Preset/Custom toggle, correct list per event, checkboxes submit via the existing native `FormData` flow with no extra wiring needed, zero horizontal overflow at 375/768/1440px, zero new console errors. Note: `event-addons-data.js` needs to be kept in sync by hand if `events.html`'s add-on lists or `pricing.html`'s package list/prices change later — it's a static data file, not derived at runtime.

- [x] Reformatted `about.html`'s "Planning policies" numbers/titles (2026-08-24) — user felt the small tracked-gold numeral + tight 8px spacing to the title (the `.rule-card` component, a deliberate prototype-fidelity choice from the original redesign) didn't look good. Switched to the same `.numbered-item` component already used earlier on the same page ("Experience that carries over" section) — big bold gold serif numeral, more breathing room before the title. Also removed the now-fully-unused `.rule-grid`/`.rule-card` CSS from `redesign-components.css` since nothing references it anymore. This is a deliberate deviation from the original design prototype, not a fidelity bug.
- [x] Swapped the 360° photo booth out of the `pricing.html` "Game Day" add-on package (2026-08-24) — user flagged it as too expensive. Researched real current Indianapolis vendor rates rather than guessing: Garcia Party Rental quotes their 360 booth at $495/2hr (real market range is $700–$1,500/2-3hr elsewhere, so the original $795 total wasn't unreasonable for the booth alone, but the user still wanted it gone). Replaced with a snow cone cart (real anchor: Garcia Party Rental's plain snow cone machine is $125/day + ~$1/serving supplies, though a full attended "cart" would run somewhat higher — treat this as a planning estimate, not a verified vendor quote the way the booth number was). New package: snow cone cart (50 servings) + two lawn game sets + 5 ft balloon garland, **$550** (down from $795). The two lawn-game-set and garland costs were not independently re-verified.
- [x] Removed copyrighted-broadcast-content exposure from event add-ons (2026-08-24) — user cannot play copyrighted media (e.g., live sports broadcasts) as part of an event. Found and fixed the one real instance: March Madness Watch Party's "Large-screen television rental" add-on implied showing the live NCAA broadcast — replaced with "Basketball-themed photo backdrop." The event's own "What's included" list was already broadcast-free by design (arcade basketball game + bracket competition, not an actual TV broadcast), so only the add-on needed fixing. Swept all 20 events for similar risk (searched television/screen/movie/projector/watch party/streaming/broadcast) — the only other hit was Trivia Night's "Television or display setup," which is fine as-is since it displays Community Spark's own trivia questions, not licensed broadcast/streaming content.

## Now
- [x] **Quote Calculator: real cross-device sync, a full 21-event pricing/needs review, and 4 tier rebalances (2026-09-01)** — Replaced the old GitHub-snapshot bootstrap (one-way, manually-exported, had gone stale) with real sync: a new `worker.js` adds `GET/POST /api/quotes` to the `community-spark-calc` Worker, backed by a new KV namespace, so every save/update writes through immediately and every device reads current shared state on load (merge by an `updatedAt` timestamp, not blind overwrite). Then did a genuine line-by-line "what do we actually need" pass across all 21 presets, not just a documentation-vs-preset check: fixed Community Pool Party (site said ~100 guests + Kona Ice, preset was costed for 65 with no Kona Ice at all — bumped to 100 guests, dropped the Kona Ice promise from the copy instead of costing it, per user direction, since it's third-party pass-through and can be added case-by-case); sourced a real local Indianapolis-area coffee roaster (Indie Coffee Roasters, Carmel IN) for National Coffee Day's "local roasted" claim; added missing paper goods (plates/cutlery/napkins) to 9 events across two passes once a standing rule was set ("any event with food needs all three, any event with Coke/Pepsi needs both diets + water") — added a real Diet Coke variant to the catalog (Pepsi already had one) and a real Diet Crush orange soda after auditing every soda family for the gap; gave Resident Fitness Session a real trainer fee (first deliberate break from the site's "no staffing lines" pattern, since this event's copy is explicitly "trainer-led"); fixed Waffle Bar (promised chocolate chips, never costed) and Patio Decorating Contest (promised a watering can + plant tags as prizes, only plant ties existed). Finished with a margin review across all 21 and rebalanced 4 tiers to match real complexity: Waffle Bar and National Bagel Day Bar moved Glow→Spark, March Madness moved Spark→Glow (was the catalog's second-lowest margin at 46.6%, now 65.8%) — on top of the Ice Cream Day/Donut Day/Dog Adoption moves logged below. Every change here is deployed live on the actual Cloudflare Worker, not just committed to the repo — that Worker doesn't auto-deploy from GitHub pushes, so `wrangler deploy` from inside `quote-calculator/` was run after each batch.
- [x] **Quote Calculator: catalog grew to 21 events, a real preset-loading data-loss bug found and fixed (2026-09-01)** — Bingo Night added as the catalog's 21st event (Spark tier, real $213 cost); deep-dive cost pass on 3 under-scrutinized presets found real gaps between what `events.html` promised and what the calculator was actually costing (National Ice Cream Day was missing cones/bowls/flavors entirely; Dog Adoption was missing its own advertised "gift bags"/"take-home bowls"; National Donut Day was missing decaf coffee and all paper goods) — all fixed with real sourced items, then further expanded per request (assorted donut trays + Munchkins-style holes, a full sundae topping bar, Dog Adoption's "fillers" split into separately-priced bags/toys/treats plus real PetSmart gift cards). Ice Cream Day and Donut Day moved Glow→Spark, Dog Adoption moved Spark→Glow to match actual complexity (site-wide: `events.html`, `book.html`, `partnerships.html`'s tier example, calculator preset `charge` values). March Madness got a real "bracket printouts" add-on (in-house printed, real copy-paper cost) and the two-player arcade game's cost was zeroed out (owned equipment, not a per-event rental) — while in that preset, also caught and fixed a standing-rule violation (Coke served without Pepsi). `pricing.html`: removed an unapproved "(multi-day formats available)" line from the Glow tier and fixed the tier price ranges so the "–1,100"/"–1,750"/"–3,000+" suffix renders at the same size as the base number instead of noticeably smaller. **Real bug found and fixed:** the "Load event preset…" dropdown always read from the hardcoded original catalog data, so editing a preset and clicking "Update saved quote" appeared to silently revert itself the next time that event was loaded from the dropdown (it hadn't — the edit was saved fine, just shadowed). First fix attempt was too broad (would have made every auto-seeded, never-touched preset permanently ignore future catalog updates); corrected to only defer to a saved copy that has a real save date, not the "Built-in preset" seed sentinel — verified both cases live with the GitHub bootstrap fetch blocked for a deterministic test.
- [x] **Mobile navigation was completely broken sitewide — fixed 2026-08-31** — a duplicate, unconditional `.mobile-toggle{display:none}` rule in `redesign-components.css` (added during an earlier "corrected mobile drawer" pass, missing its own media query) was silently overriding `styles.css`'s working responsive rule on every page that loads both files — every real page except `404.html`/`privacy.html`. Below the 920px breakpoint this meant both the desktop nav AND the hamburger menu were hidden at once — real phone visitors had **no way to navigate off whatever page they landed on**. Found via a real mobile-width Playwright check after the pricing-range update below, not caused by it. Fixed by merging the intended "corrected" visual values into `styles.css`'s single rule (which already had the right `@media(max-width:920px)` toggle) and deleting the conflicting duplicate. Verified across all 11 pages at 375px: hamburger renders, drawer actually opens on click, active page highlights correctly, zero console errors.
- [x] **Tier pricing changed from flat "+" numbers to real bounded ranges (2026-08-31)** — Spark $800–1,100, Glow $1,250–1,750, Radiance $1,750–3,000+ — across pricing.html, index.html, contact.html's budget dropdown, and book.html's event dropdown, so a bigger-budget property can see there's real room above the old ceiling. Per-event cards on events.html now lead with the tier name (Spark/Glow/Radiance) plus the range in parenthesis instead of a raw dollar figure, matching the filter buttons (now labeled by tier name too). Calculator preset data (`QB_EVENT_PRESETS`) is untouched by design — each booking still gets one real number; ranges are a marketing-page concept, not a data model change.
- [x] **Hosting decision, permanent (confirmed 2026-08-24): Cloudflare (`community-spark-events.newtongriffinjames.workers.dev`) is the main/primary site going forward; Netlify is the backup.** Originally framed as temporary ("wait out the Netlify free-tier cap until mid-September") but the user made it permanent regardless of Netlify's cap status. Both stay git-connected and auto-deploy from the same repo on every push to `main` — no workflow change day to day. Netlify's free-tier cap (hit 2026-08-20) is no longer being tracked as a blocker since Cloudflare doesn't depend on it.
- [x] **Cloudflare's custom `404.html` wasn't being served** (found 2026-08-24, fixed 2026-08-24) — confirmed via direct request: a broken URL on the Cloudflare deploy returned an empty 404 body. Root cause: no `wrangler.jsonc` existed in the repo, and Cloudflare's Git-integration deploy needs one to set `assets.not_found_handling: "404-page"` (found via Cloudflare's own docs, user provided the exact config needed). Added `wrangler.jsonc` at repo root (`name: "community-spark-events"` — confirmed by user against the dashboard, `directory: "./"` to match how the whole repo is already served, same as `netlify.toml`'s `publish = "."`). Should take effect on the next push since the Worker is git-connected. **Verify after next deploy**: request a broken URL on the Cloudflare domain and confirm the styled 404 page shows, not an empty response.
- [x] **Contact/Sponsor/Booking forms broken on Cloudflare — fixed 2026-08-27** (flagged 2026-08-24) — all 3 forms used Netlify's built-in form handling (`data-netlify="true"`), which silently failed once Cloudflare became the permanent host. **Fix:** migrated all 3 (`contact.html`, `sponsors.html`, `book.html`) to Web3Forms — user got a free access key, `script.js`'s `submitWeb3Form` now POSTs JSON to `https://api.web3forms.com/submit`. Verified end-to-end with a real test submission confirmed delivered. Also found and fixed a real regression during the migration: `Object.fromEntries(formData)` silently drops repeated form-field values (e.g. multiple checked checkboxes) — replaced with a `formDataToObject()` helper that aggregates repeats into a joined string, verified via a standalone Node test.

- [x] **Google Places API key was exposed client-side (2 successive keys, both git-history-exposed) — fixed 2026-08-27** — removed the key from client code entirely; address autocomplete now calls a new Cloudflare Worker proxy (`community-spark-places-autocomplete.newtongriffinjames.workers.dev`, code in `cloudflare-worker/`) that holds the real key as a Worker secret and enforces an origin allowlist. Verified live: 200 + real suggestions from the allowed origin, 403 for disallowed origins. Both old exposed keys deleted by the user in Google Cloud Console.
- [x] **`.git`/`.netlify/db`/`.claude` were being served as public static assets on the Cloudflare deploy — fixed 2026-08-27** — root cause was Cloudflare Workers' static-assets deploy model having no `.gitignore` awareness (it uploads everything under `assets.directory`); had been live since at least 2026-08-24. Fixed with a new `.assetsignore` file excluding all non-public paths (`.git`, `.netlify`, `.claude`, `.wrangler`, internal-only markdown/design folders, etc.). Verified via direct curl that those paths now 404 while the real site still serves normally. (Surfaced and fixed while recovering from an unrelated incident where the `community-spark-events` Worker was mistakenly deleted mid-session and had to be restored — full site + config recovered, no lasting impact, but flagging here since the underlying exposure predated that incident and was independent of it.)
- [x] Second full-site content/consistency/accessibility review pass (2026-08-27, mirrors the 2026-08-24 pass) — fixed and verified live in commit `7732f0b`: `about.html` founder bio replaced with real bio info, remaining "staffing" mentions removed sitewide, `privacy.html` corrected to reflect the Web3Forms/Cloudflare-proxy changes above, title-case/response-time consistency fixes on `contact.html`, several accessibility fixes (`gallery.html` double-announced captions, `events.html` search icon, ARIA labels on calculator time slots), and a commented-out (inactive) social-links footer block added to all 11 pages ahead of real accounts existing.
- [ ] Domain: user will purchase one themselves (see "Found during a roadmap review pass" below for registrar recommendations). **All domain-dependent work — DNS pointing, canonical/`og:url`/`sitemap.xml`/`robots.txt` metadata currently hardcoding the Netlify URL — is intentionally on hold until after purchase**, so it's done once against the real domain rather than churned through an interim Cloudflare-subdomain state first.
- [x] Push new site design across all 8 pages (2026-08-20)
- [x] Fix Contact/Sponsor form confirmation — was resetting silently instead of showing a clear "sent" panel (2026-08-20)
- [x] Visual QA pass on the redesign (2026-08-21) — screenshotted all 8 pages at mobile (375px) and tablet (768px), reviewed for overflow/layout/console errors. Note: the site's scroll-triggered `.reveal` animations (motion.js `inView`) don't fire under a plain `window.scrollTo`/headless fullPage screenshot — only real wheel/touch-driven scroll events trigger them. Confirmed this is a testing-method artifact, not a live-user bug (verified via `page.mouse.wheel()` simulating real scroll input). Found and fixed 2 real mobile-only overflow bugs, both root-caused to bare `1fr` grid tracks / unconstrained form elements missing `minmax(0,1fr)` / `width:100%;min-width:0` (same class of bug as the numbered-grid alignment fix earlier): `.form-layout` in `redesign-components.css` and `.field-group input/select/textarea` in `styles.css`, affecting the Contact and Sponsors forms. Also converted Sponsors' "Business Type" field from a fixed-category `<select>` (whose longest option was the actual overflow trigger) to a free-text input, per user direction — applies on both mobile and desktop. Verified zero horizontal overflow and zero console errors at 375px/768px/1440px on both pages after the fix. Everything else across all 8 pages at both breakpoints rendered clean.
- [~] **Redesign port turned out to be a loose reimplementation, not a faithful port** (found 2026-08-20) — the "Push new site design" item above shipped different class names/structure/content hierarchy than the actual prototype (`Community Spark Website Redesign/design_handoff_website/`), not just a styling drift. Confirmed by comparing rendered prototype (screenshot) against live site. User asked for a proper page-by-page rebuild against the real prototype markup/CSS instead of a patch job.
  - Built `redesign-components.css` + `redesign-components.md` at repo root — a faithful CSS port of the prototype's shared component library (buttons, cards, hero, header/footer/drawer, forms, etc.), extracted directly from the prototype's `_ds_bundle.js`. Load this after `styles.css` on any page being rebuilt. The header/nav/footer/announcement bar/buttons were already faithful pre-existing — only the mobile drawer needed a real fix (wrong panel bg/padding, no exit animation, no active-page highlight); that fix is in the new file, reuses existing `script.js` hooks, no JS changes needed.
  - [x] Events page (`events.html`) rebuilt and user-approved (2026-08-20): removed the photo/emoji blocks (prototype has none), moved price to always-visible on the closed card (was hidden inside the expandable details — a real UX regression, not just styling), added the "Showing X of Y events" counter + Clear filters (prototype has these, live didn't), restyled the toggle to the prototype's circular +/– button, fixed category filter order to match prototype, replaced the dark `.budget-banner` sponsor callout with a white `.sponsor-note` card matching the prototype. Also fixed a real content bug found along the way: all 20 event cards were missing their description text entirely (dropped during the original port, not a redesign issue).
  - [x] Pricing page (`pricing.html`) rebuilt (2026-08-20): converted hero to `.page-hero`, switched tier cards to the new `.price-card-*` component classes. **Real content decision, not just styling:** the prototype replaces the live 12-item à la carte add-on calculator (individual quantity dropdowns + live running total) with 4 static curated bundle cards (no interactivity — the prototype's own estimator/PDF-download code is feature-flagged off). Per user direction, switched to the bundle cards; the prototype's 4th bundle ("Music & Energy") was DJ-based so it was rebuilt DJ-free as "Game Day" (360° photo booth + 2 lawn game sets + balloon garland, same $795 price point, priced from real existing add-on rates already on the site — not invented). PDF export intentionally not built (see "Low priority" below).
  - [x] Partnerships page (`partnerships.html`) rebuilt (2026-08-20): confirmed and fixed the heavier restructuring flagged below. Converted hero to `.page-hero`. Replaced the bordered `.process-steps` cards (used for "Why property teams book" / "How it works") with a new faithful `.numbered-grid`/`.numbered-item` pattern matching the prototype's plain numbered-card style (no colored top-border cycling, no circle-badge numbers). Replaced the "look at the catalog" section's `.catalog-grid`/`.event-card` markup (which had non-prototype photo/emoji placeholder blocks — same issue Events had) with a new faithful `.sample-grid`/`.sample-card` pattern. Also fixed a real hairline drift in the shared `.funding-block` component (used here and on sponsors.html): label color was gold, prototype spec is muted gray — corrected in `styles.css`.
  - [x] Sponsors page (`sponsors.html`) rebuilt (2026-08-20). **Real content decision, not just styling:** the live page published specific sponsorship dollar ranges per level ($250–$500 / $600–$1,000 / $2,000–$5,000); the actual prototype removes all pricing and shows scope only ("One element" / "One event" / "3–6 events"), framing it as "your exact amount is set when we match you to a specific community and event." Per user direction, matched the prototype — no dollar amounts published anywhere on the page now (cards, hero proof line, and the form's tier-interest options all updated). Also removed two sections that don't exist in the actual prototype at all: the `.partner-grid` "how you show up" cards and the `.budget-banner` "sponsoring more than once" panel — both were extra content from the original loose port, and both were already redundant with the form's own attend/frequency questions and the Community Partner level card. Converted hero to `.page-hero`, moved the top stats strip from the dark `.trust-bar` to the same `.tier-strip` pattern Partnerships uses (matches the actual prototype component), added new shared `.level-card`/`.level-grid` and `.form-layout`/`.contact-line` components to `redesign-components.css`, and promoted the `.numbered-grid`/`.sample-grid` patterns (added for Partnerships) into the shared file since Sponsors reuses them too. No closing panel at the end — the prototype doesn't have one on this page, the "Join the list" form is the closing CTA.
  - [x] Gallery page (`gallery.html`) rebuilt (2026-08-20): switched the masonry `.gallery-mosaic` column layout to the prototype's uniform fixed-height `.photo-grid` (new shared component in `redesign-components.css` — distinct from the unused bundle `GalleryItem` component already there), with the "Stock image" tag combined into the caption bar instead of a separate floating badge. Converted hero to `.page-hero`. Content/copy already matched exactly.
  - [x] About page (`about.html`) rebuilt (2026-08-20): converted hero to `.page-hero` (kept `id="about"` on it — `motion-effects.js` gates the FAQ height-animation and button press-effect behind that id, would have silently broken otherwise). Switched the founder section from the existing `.founder-card` bundle component (cream bg, flex, plum 56px initials) to a new faithful `.founder-block` (white bg + border/shadow, grid layout, ink 88px initials with a gold ring — the real prototype doesn't use the bundle component here either). Replaced `.experience-row`/`.policy-card` with the shared `.numbered-grid`. Replaced the "planning policies" `.policy-grid`/`.policy-card` (44px pale watermark numerals) with a new faithful `.rule-grid`/`.rule-card` (small bold gold numeral badge, responsive auto-fit grid) — same "page doesn't actually use the component its name implies" issue as founder/gallery. All 4 section heads switched from `.section-center` to `.section-head` (split), matching the established default from every other rebuilt page. Copy already matched exactly.
  - [x] Contact page (`contact.html`) rebuilt (2026-08-20) — **last page, faithful redesign pass now complete across events/pricing/partnerships/sponsors/gallery/about/contact.** Switched to the `.form-layout`/`.form-side`/`.contact-line` pattern (4 sidebar cards: Email, Phone, "Local business? → sponsors.html", "Response time"), matching sponsors.html's already-established structure. Sponsor-support checkbox now checked by default, matching the prototype. Fixed the event-location help text to match the prototype's copy exactly. **Correction to an earlier note in this file:** the "4th budget option" flagged below as a non-prototype addition was wrong — re-checked the actual `contact-app.jsx` `BUDGETS` array and it's genuinely in the prototype; no change needed there, it was already faithful. **Real functional addition, not just styling:** implemented the prototype's `?event=`/`?intent=` deep-link prefill behavior, which didn't exist anywhere on the live site before — arriving from an event card's "Ask a Question" link now shows a banner naming the event, changes the section/form heading, and pre-fills the message field (logic added to `script.js`, guarded behind `contactForm`'s existence so it's contact.html-only; new `.prefill-banner` component in `redesign-components.css`). Updated all 20 "Ask a Question" links in `events.html` to actually pass `?event=<name>&intent=question` (previously plain links with no context) — purely additive, doesn't touch the separate `book.html` calendar flow.
  - [x] Home page (`index.html`) rebuilt (2026-08-20) — **all 8 redesign pages now faithfully rebuilt.** Was wrongly assumed already-faithful; a direct rendered-prototype-vs-live screenshot comparison (same method that caught the original loose-port issue) confirmed real gaps: `home-app.jsx` actually calls the same shorter interior-page `PageHero` used everywhere else (not a distinct full-viewport hero), and its 3-item feature grid is `repeat(3,1fr)` with an `is-large` modifier that's a no-op in the prototype's own CSS. Converted hero to `.page-hero`, dropping the old JS-driven Ken-Burns zoom-on-load effect (`id="heroBg"` + `script.js`'s scale-in) since the prototype doesn't do anything special for Home's hero — matches every other page's plain static background now. Fixed a small copy drift in the hero lead ("local sponsorship opportunities — all built..." → prototype's "local sponsorship options — built..."). Replaced `.steps-grid`/`.step` (single hairline-divided block) with a new faithful `.step-tile-grid`/`.step-tile` (individually gapped/bordered cards). **Real content decision, confirmed with user:** kept the "Featured experiences" tiles pointing at real catalog events with real prices (Fall Festival/Trivia Night/Community Pool Party) rather than the prototype's generic marketing copy — this was a deliberate earlier fix (its own commit: "Fix homepage featured experiences to reference real catalog events"), not a drift — but fixed the layout from the old asymmetric 1.4fr/.6fr grid to the prototype's equal 3-column grid via a new `.feature-showcase` component. Switched the "Transparent pricing" section head from centered to split, matching every other page's default. Book (`book.html`) remains intentionally excluded — its own real calendar/conflict-checking flow, not something the design prototype models at all.
- [x] Image optimization — photos now served as WebP with JPEG fallback + explicit width/height (2026-08-20). Actual win was ~23% smaller total image weight (876KB→~678KB for WebP-capable browsers) — the originals were already reasonably compressed, so this was a real but modest gain, not the dramatic one first estimated.

- [x] Cloudflare set up as a second parallel deploy target alongside Netlify (2026-08-21) — went live via Cloudflare Workers static assets (not classic Pages — dashboard flow has shifted), URL: `community-spark-events.newtongriffinjames.workers.dev`. Verified homepage renders fully (nav, hero, featured events w/ images, pricing tiers, footer) with no broken elements. Reason: Netlify free-tier usage cap was hit and blocking deploys (see item above); Cloudflare's free tier has unlimited bandwidth so it avoids that problem going forward. Git-connected and auto-deploying from the same repo on every push to `main`, same as Netlify. Domain stays pointed at Netlify for now, no cutover decided. Note: the site's `data-netlify="true"` contact/sponsor forms ([contact.html:47](contact.html#L47)) only work when served from Netlify — they will silently fail on the Cloudflare copy until swapped to a third-party form backend (Web3Forms suggested), so don't point real domain traffic at the Cloudflare deploy until that's done.
- [x] Pricing tiers changed to "starting at" ($800+ / $1,250+ / $1,750+) (2026-08-24) — added the `+` suffix everywhere a tier price is displayed (pricing cards, homepage tier strip, event catalog cards and filters, contact budget dropdown, book.html, partnerships) to make clear these are starting prices, not flat rates. Applied site-wide including the 7 marketing flyer concepts and the leave-behind flyer's embedded doc, matching the `$1,750+` treatment the Radiance tier already had before this pass.
- [x] Real logo lockup swapped in site-wide (2026-08-24, completed 2026-08-24) — replaced the placeholder ✦-circle + text brand mark with the user's actual "Community Spark" wordmark lockup (`images/community-spark-primary-lockup.png`, trimmed from their original upload). Updated header + footer on all 10 pages (footer uses a `brightness(0) invert(1)` CSS filter since its dark background would otherwise swallow the logo's dark text), the Quote Calculator tool (embedded as a base64 data URI to preserve its offline single-file design), and the 4 marketing flyer concepts that actually used the icon+wordmark pattern (Main, Blueprint, ConceptB, Postcard — Editorial/Gazette/TicketStub use the brand as typography or a decorative motif instead, not a swappable logo, so left as-is). Also fixed the 2 functional logo occurrences embedded inside `marketing/community-spark-leave-behind.html`'s JSON doc (its Main/Blueprint board copies) by operating directly on the raw escaped JSON text — the doc's own 3rd star icon (its TicketStub board copy) is a decorative seal, not a logo, left alone same as the standalone file.

- [x] Fixed live copy that undersold "we run the event" (2026-08-24, extended 2026-08-24) — found while reviewing the site for roadmap gaps: `partnerships.html`'s hero lead listed "plans, sources, sets up, staffs, and breaks down" with no verb that actually says Community Spark runs the event (the h1 above it, "Resident events your team doesn't have to run," implies it but the lead paragraph never delivered on it). Changed to "plans, sources, staffs, and runs... — setup and breakdown included." Same issue on `pricing.html`'s tier-section intro ("planning, setup, on-site coordination, and breakdown" → "...our team running the event on-site, and breakdown"). Matches the standing instruction that site copy must show Community Spark performs the event, not just plans/sets up/tears down. **Follow-up sweep (2026-08-24):** user asked specifically that every "setup and breakdown"-style mention across the live site also make clear Community Spark physically runs the event. Fixed 5 more spots that only mentioned setup/coordination/breakdown without ever saying "runs": `index.html`'s hero proof pill ("Setup and breakdown included" → "We run every event"), its "Built for property teams" intro paragraph, and its step-4 "Enjoy" tile (the literal event-day step — most important one to fix); `partnerships.html`'s hero proof pill and its "Your team stops running events" bullet ("event-day coordination" → "on-site hosting"). Left alone, on purpose: `partnerships.html`'s "We run the event" numbered item (heading already says it explicitly), `pricing.html`'s per-tier "Setup and breakdown" bullet (covered by the "our team running the event on-site" sentence directly above it), and `events.html`'s per-event "Market layout, setup, and breakdown" bullet (item-specific inventory list, not a service-model statement). The same phrasing is still present in most marketing flyer drafts — still not a priority per the user (2026-08-24), they don't expect to use those concepts. **Second follow-up (2026-08-24):** user asked pricing.html specifically state this applies no matter which tier — changed the "Included in every tier" list's "On-site coordination" item to "We run the event on-site" (this list is explicitly the "true across every tier" section, so this was the right single place to fix rather than editing each tier's bullets separately). Also removed The Glow tier's "Additional staffing as needed" bullet per the user — now redundant/contradictory once every tier explicitly includes full on-site running of the event, since it implied only the mid tier got full staffing. **Third follow-up (2026-08-24):** user flagged the "Photo Corner" add-on package ($420) listed a "Lighted marquee accent" they don't actually have — removed rather than replaced with a different specific prop, to avoid fabricating another unverified inventory claim. Package now lists 2 items (photo backdrop, welcome sign) instead of 3; price left unchanged since the user didn't ask for a price adjustment — worth revisiting if $420 no longer reflects the smaller item list.
- [x] Nav order changed to Home, Pricing, Events, Communities, Sponsors, Gallery, About (2026-08-24) — swapped Events and Pricing (previously Home, Events, Pricing, ...). Applied to both the desktop nav and mobile drawer nav (which uses different link text, "Event Catalog") on all 11 pages including 404.html; the per-page `active` class stayed correctly attached to whichever link it was already on. Verified nav text and zero console errors across all 11 pages.

## Later — fix but not urgent
- [x] Install `wrangler` CLI (2026-08-23) — installed globally via npm (v4.125.0). Not logged in yet (`wrangler login` is a browser sign-in, needs to be run by the user). Two optional install scripts (esbuild, workerd) were skipped by npm's allow-scripts guard — only affects `wrangler dev` (local preview), not deploys or account commands; can be enabled later if local preview is needed.
## Quote Calculator (internal tool) — built 2026-08-21
Real build shipped at `quote-calculator/quote-calculator.html` — a single self-contained offline file (React/ReactDOM/Babel embedded inline, ~3.4MB, no server/build/internet required). Still works standalone (double-click or `file://`) for local use.
- [x] **Now also hosted privately for cross-device access (2026-08-30)** — deployed as its own separate Cloudflare Worker, `community-spark-calc` (config in `quote-calculator/wrangler.jsonc`, entirely independent from the main site's `community-spark-events` Worker — different name, different config, doesn't affect the live site). Live at `https://community-spark-calc.newtongriffinjames.workers.dev`, gated behind **Cloudflare Access** (Zero Trust) restricted to `newtongriffinjames@gmail.com` — verified via direct request that an unauthenticated hit gets redirected to Access's login (302 to `*.cloudflareaccess.com`), covering the whole app including `saved-quotes.json`, not just the homepage. `quote-calculator/.assetsignore` excludes `.wrangler` and the Worker config itself from the public asset upload (a first deploy briefly exposed the local Wrangler cache, including the Cloudflare account id/email — caught and fixed same session before it mattered). The original file-based/offline workflow still works unchanged; this is an additional access path, not a replacement.
- [x] Feature scope: per-store delivery minimums (Costco $75 / Sam's $50 / BJ's $50 / Amazon $35 non-Prime, real published thresholds) with a warning on each shopping-list card when under the minimum; deposit/payment schedule (50% at confirmation, 50% at event completion — real policy from user, editable per-quote for approved exceptions). Waste/buffer % and tax exemption intentionally not built.
- [x] In-app price editor ("Edit catalog prices" button) — lets you correct/fill in any item's price at any store directly in the tool, persisted to localStorage as overrides on top of the base catalog, no code editing or re-download needed. Adding an entirely new catalog item/family (not just a price) still requires messaging Claude for a code change.
- [x] Pricing research: real current Amazon + Costco prices filled in where a genuine live listing was found (~35 of ~65 priced line items). Sam's Club/BJ's are null everywhere (no public pricing source exists, confirmed by direct search) — fill those in yourself via the price editor. Costco.com actively blocks automated price checks, so Costco coverage is thinner than Amazon's; a few Costco figures came from third-party warehouse-club price-tracking sites rather than costco.com directly (flagged in the data file's header comment). Anywhere neither store had a confirmed price, it's left `null`/$0.00 on purpose rather than guessed — a placeholder that looks plausible is more dangerous than an obvious $0, since it could slip into a real client quote unnoticed.
- [x] **Business-reality finding, not just a data gap:** Costco's national catering program has shrunk to ~4 standard platters and no longer includes a taco bar, BBQ platter, pasta bar, or continental breakfast package as an orderable item. The 4 matching "Catering" families in the tool are priced null for that reason — if you want real numbers there, they'd need to come from an actual local caterer, not Costco/Amazon.
- [x] Hosting/persistence: single local file, localStorage only, no backend, no sharing — supersedes the earlier "separate small deploy" decision.
- [x] Sam's Club + BJ's real pricing added (2026-08-23) — user compiled their own membership pricing into a spreadsheet (`Sams Club + BJ's prices.xlsx`, 19 sheets) and had it imported directly into the catalog, superseding the "null everywhere, fill in yourself" state from the initial build. Matched on base product name only (ignored size/qty/price text embedded in some sheet titles); BJ's sheet prices were stored with no decimal point (e.g. `$1099`) and were converted to `$10.99` per the user's confirmed rule. Capped every catalog item to a max of 3–4 pack-size options per the user's instruction, which required restructuring two families to the sizes actually available in the spreadsheet: napkins (was 500/1000ct → now 504/660/800ct) and the deli sandwich/catering platter (was serves-24/48 → now serves-12–15/20–25). Added a new "Bulk candy" family under Food (30ct bars, 62oz jar) — user confirmed. Where the spreadsheet had no clean match to an existing pack size, prices were left `null` rather than guessed (lemonade, iced tea, coffee/cocoa service, pretzels, cookies, popcorn, fruit/watermelon trays, ice cream, hot dogs, buns, burger patties — none present in the spreadsheet). Coverage is still partial — only spreadsheet items were filled in, remaining gaps are unchanged from the original null state.

## Low priority / skip unless it matters
- [x] Site-wide type-scale "readability" bump from the redesign (2026-08-24) — was held off since 2026-08-21 pending the mobile/tablet visual QA pass, which is now done. Bumped the whole UI type scale in `redesign-components.css` by +1px (`--text-lead` 18→19, `--text-body-lg` 17→18, `--text-body` 15→16, `--text-body-sm` 14→15, `--text-meta` 13→14, `--text-micro` 12→13, `--text-label` 11→12, `--text-eyebrow` 10→11 — display/heading sizes left untouched), plus the 10 hardcoded 15px body-copy declarations in `styles.css` (search input, form fields, FAQ summaries, about/founder copy, closing panel, split cards, etc.) that predate the token system, bumped to 16px for consistency. This is a deliberate deviation from the design prototype's own type-scale spec (`tokens/typography.css`), not a fidelity fix — prototype reference files left untouched. Verified via a fresh QA sweep (all 10 pages × 375/768/1440px): zero new horizontal overflow, zero new console errors.
- [x] Gallery page masonry → uniform grid — done as part of the gallery.html rebuild (2026-08-20)
- [ ] Pricing page "download as PDF" feature from the design prototype — explicitly skipped (2026-08-21): would add a new CDN dependency (jsPDF) for a feature not currently on the live site; user chose to keep the site dependency-light.

## Found during a roadmap review pass (2026-08-24)
- [x] No custom 404 page → **built and confirmed live on Cloudflare 2026-08-24** (`404.html` + `wrangler.jsonc`, see "Now" above for the fix).
- [x] `FAQPage` structured data added to `about.html` → **done 2026-08-24** — all 11 existing FAQ Q&As from the page, makes them eligible for Google's FAQ rich results.
- [x] `sitemap.xml` `lastmod` dates added → **done 2026-08-24** (all 9 URLs, dated 2026-08-24).
- [x] Branded Open Graph share image → **done 2026-08-24** (`images/og-share.jpg`, 1200×630, built to match the site's design system — gold rule, serif wordmark, tracked-caps tagline) — replaces the reused event photo (which cropped awkwardly at the 1.91:1 OG ratio) as `og:image` across all 9 indexed pages.
- [ ] `Event` structured data for `events.html` — bigger lift, intentionally held off until real event *dates* exist rather than the generic preset catalog (schema.org's `Event` type wants a real `startDate`).
- [x] No "skip to main content" link → **fixed 2026-08-30** — added a `.skip-link` (visually hidden until focused, first focusable element in `<body>`) pointing at `#main-content` on all 11 pages, and gave every page's `<main>` that id. Verified: exactly one skip-link + one `id="main-content"` per page, no duplicates or misses.
- [~] Social media links — infrastructure ready, not live yet (2026-08-27). User confirmed no accounts exist yet. Added a `.footer-social` component (`styles.css`) and a commented-out Instagram + Facebook icon block in `footer-bottom` on all 11 pages, with `COMMUNITYSPARK_HANDLE` as the placeholder in each URL. **To activate:** once real profiles exist, uncomment the block on all 11 pages and replace `COMMUNITYSPARK_HANDLE` with the real handles — a find/replace across all files, no new build needed.
- [ ] No mention of Community Spark carrying its own liability insurance — every "insurance" mention on the site is about *vendor/property* insurance requirements, never Community Spark's own coverage. For a vendor running physical events on a client's property, "fully insured" is a real trust signal property managers commonly look for. **Answered by user (2026-08-24): does not carry it yet, but plans to.** Do NOT add any "insured"/"fully insured" language to the site until the user confirms coverage is actually in place — revisit this item once that changes.
- [ ] Gallery page still uses stock photos (tagged as such in the caption) — swap in real event photos once there's a backlog from actual bookings.
- [ ] The "plan, source, set up, staff, break down" copy gap (see the "Fixed live copy" item above) is still present in most marketing flyer drafts (Blueprint/ConceptB/Gazette/Main/Postcard/TicketStub) — only the live site (`partnerships.html`/`pricing.html`) was fixed. Explicitly **not a priority** — user doesn't expect to end up using these flyer concepts (2026-08-24).
- [ ] Privacy policy explicitly states "no cookies, no analytics, no trackers, nothing to opt out of" — worth remembering before ever proposing visitor analytics (even a cookieless option like Cloudflare's free Web Analytics), since it's a real published commitment, not just an unused option.

## Possible future automation (not started — revisit once the business has real volume)
- Scheduled bot to work through this roadmap autonomously (needs PR-review guardrails, not direct-to-main pushes)
- Scheduled bot to review the live site + named competitors and propose redesign/feature ideas
- Later, once there's real outreach volume: n8n-based automation for email follow-ups, property outreach, event ideation, P&L/pricing monitoring — decided to hold off on this until the business picks up (2026-08-20)
